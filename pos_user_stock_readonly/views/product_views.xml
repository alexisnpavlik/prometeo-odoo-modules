<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Heredar la vista de formulario de product.template del módulo product -->
    <record id="view_template_only_form_inherit_pos_stock_readonly" model="ir.ui.view">
        <field name="name">product.template.stock.form.inherit.pos.stock.readonly</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button type="object"
                        name="action_open_quants"
                        class="oe_stat_button"
                        icon="fa-cubes"
                        invisible="not is_storable"
                        groups="pos_user_stock_readonly.group_pos_user_stock_readonly">
                    <div class="o_field_widget o_stat_info">
                        <field name="qty_available" widget="statinfo" string="A mano"/>
                    </div>
                </button>
            </xpath>
        </field>
    </record>

    <!-- Heredar la vista de formulario de product.product (variante) del módulo product -->
    <record id="product_normal_form_view_inherit_pos_stock_readonly" model="ir.ui.view">
        <field name="name">product.product.stock.form.inherit.pos.stock.readonly</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button type="object"
                        name="action_open_quants"
                        class="oe_stat_button"
                        icon="fa-cubes"
                        invisible="not is_storable"
                        groups="pos_user_stock_readonly.group_pos_user_stock_readonly">
                    <div class="o_field_widget o_stat_info">
                        <field name="qty_available" widget="statinfo" string="A mano"/>
                    </div>
                </button>
            </xpath>
        </field>
    </record>

</odoo>
